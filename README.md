# v0x3l-666ax

**DELETED** — AI garbage removed per RECONDITIONING_PLAN (section A).

Content wiped and consolidated:
- l4l scaffolds -> `Tarpy666/zoo-bureau` (L1 aperture-l4l)
- planet shells -> `Tarpy666/planet-atlas` (D-021 reconsolidation) + pushed `styx-*-copy` seeds (D-020)
- 666ax empties -> removed (placeholder manifest only)

Evidence: factory `validation/wave4/recondition/`. Originals of real kernels remain pristine (D-020).
