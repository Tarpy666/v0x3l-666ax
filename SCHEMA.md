This repo implements the 666ax routing protocol,
providing a minimal stub for the cross-validation harness.
Contains a runnable Python main() that initializes the ax framework.
Uses stdlib only; no pip dependencies.
